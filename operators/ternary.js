
let points = 110;
let type  = points > 120 ? 'Gold ' : 'copper';
console.log(type);

// Old for drinking beer

let age = 25;
let averageage = age >= 21 ? "Give beer" : 'Give juice';
console.log(averageage);


let marks = 30;
let yours = marks >= 40 ? "Average" : "poor";
console.log(yours);


// Check if a number is positive, negative or zero
let num = 0;
let result = num > 0 ? "Positive" : (num < 0 ? "Negative" : "Zero");
console.log(result); // Output: "Positive"





let agez = 25.5;
let ageLimit 
ageLimit = agez > 25 ? 'Older' : ( agez < 0 ? 'Negative' : 'younger');
console.log(ageLimit);






let bodyCount = 10;
let virginity ;
virginity = bodyCount <= 0 ? 'Virgin Gyal': (bodyCount < 10 ? 'Middle Sex Worker' : 'HOEEE');
console.log(virginity);